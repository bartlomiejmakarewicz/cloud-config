spring:
  rabbitmq:
    host: rabbit
  cloud:
    consul:
      host: consul
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include:
          - bus-refresh
